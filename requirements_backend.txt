# VetGPT Backend — additional dependencies (append to requirements.txt)

# API Framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Auth
python-jose[cryptography]==3.3.0   # JWT tokens
bcrypt==4.1.3                      # password hashing (direct, Python 3.12 safe)

# Database (user accounts, query logs)
sqlalchemy==2.0.31
alembic==1.13.1                    # DB migrations
aiosqlite==0.20.0                  # async SQLite (swap to Postgres in prod)

# LLM clients
anthropic==0.29.0                  # Claude
openai==1.35.0                     # GPT-4o / embeddings fallback

# Validation & config
pydantic==2.7.4
pydantic-settings==2.3.4

# Rate limiting
slowapi==0.1.9

# HTTP client (for health checks)
httpx==0.27.0


# Phase 3 — Vision Pipeline
openai==1.35.0                    # GPT-4o Vision
google-generativeai==0.7.2        # Gemini 2.0 Flash fallback
google-cloud-documentai==2.24.0   # Document AI OCR
pydicom==2.4.4                    # DICOM X-ray file support
Pillow==10.4.0                    # Image processing
numpy==1.26.4                     # Pixel array manipulation

# Bots
python-telegram-bot==21.3          # Telegram bot
twilio==9.2.3                      # WhatsApp via Twilio

# Farm management — audio transcription
faster-whisper==1.0.3              # local Whisper (free, multilingual, runs on CPU)
 
 # Scheduling & notifications
apscheduler==3.10.4                # background job scheduler
 